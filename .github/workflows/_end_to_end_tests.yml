name: end-to-end-tests

on:
  workflow_call:
    inputs:
      env_test_file:
        required: true
        type: string
      cypress_run_command:
        required: true
        type: string

jobs:
  end-to-end-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
      - name: Install dependencies
        run: npm ci
      - name: Use ${{ inputs.env_test_file }}
        if: ${{ inputs.env_test_file != '.env.test' }}
        run: mv ${{ inputs.env_test_file }} .env.test
      - name: Build the application
        run: npm run build -- --mode test
      - name: Start the application
        run:
          npm run preview -- --mode test & npx wait-on
          http://localhost:3000/humanservices/dmahs/info/doulahelp
      - name: Run cypress tests
        run: npm run "${{ inputs.cypress_run_command }}"
